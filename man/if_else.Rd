% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/if_else.R
\name{if_else}
\alias{if_else}
\title{Vectorised if}
\usage{
if_else(condition, true, false, na = NULL)
}
\arguments{
\item{condition}{A logical vector.}

\item{true, false}{Values to use for \code{TRUE} and \code{FALSE} values of \code{condition}.}

\item{na}{Values to use for \code{NA}.}
}
\value{
A vector with the common type of \code{true}, \code{false}, and \code{na}; and the size of
\code{condition}. Where \code{condition} is \code{TRUE}, the result comes from \code{true},
where \code{FALSE} it comes from \code{false}.
}
\description{
\code{if_else()} returns a vector filled with elements selected from \code{true},
\code{false}, and (optionally) \code{na} based on the value of \code{condition}. It
is a type- and size-stable version of \code{\link[=ifelse]{ifelse()}}.
}
\examples{
x <- c(NA, 1:4)
if_else(x > 2, 1, 2)
if_else(x > 2, "small", "big")
if_else(x > 2, factor("small"), factor("big"))

y <- as.Date("2020-01-01")
if_else(x > 2, NA, y + x)
}
